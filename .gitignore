node_modules/
dist/
data/
.env*
!.env.example
.dev.vars*
.wrangler/
*.sqlite
*.sqlite-*
# tsc -b's incremental state, one per project in tsconfig.json.
*.tsbuildinfo
# Agent worktrees (Claude Code process scaffolding).
.claude/worktrees/
