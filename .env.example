# Configuration for x-threaded. Every value is per-deployment: nothing here
# is baked into the code or committed to the repo.
#
#   Local dev:  cp .env.example .env and fill this in. Both local runtimes
#               read it — the Bun server natively, and `wrangler dev` too.
#               (Don't create a .dev.vars file: wrangler silently ignores
#               .env when one exists.)
#   Deployed:   set the same names with `bunx wrangler secret put NAME`. Secrets
#               persist across deploys; wrangler.jsonc vars would be
#               committed and follow anyone who forks the repo.
#
# The uncommented names below are also what the Deploy to Cloudflare button
# offers to fill in — so everything with a working default stays commented
# out, and the setup page asks only for what it genuinely can't guess.
#
# X_BEARER_TOKEN is the only credential required to start locally. A deployed
# Worker also refuses to serve its API until you configure the Access pair or
# deliberately set ALLOW_UNGATED. OAuth is optional, and tuning values have
# working defaults.

# ---- Required ----------------------------------------------------------

# App-only bearer token from https://developer.x.com — reads public
# conversations. The free tier can't do it: the account needs pay-per-use
# billing. Billed per post read (~$0.005); set a spending limit in the
# developer console before loading anything.
X_BEARER_TOKEN=

# ---- Optional: user-context features -----------------------------------

# OAuth 2.0 credentials from an X app with "Type of App" set to confidential
# client. Enables the Your posts tab and bookmark folder sync. Register the
# callback for THIS deployment on the app:
#   http://localhost:8788/auth/callback     (local dev)
#   https://<your-worker>/auth/callback     (deployed)
# Then visit /auth/login once to authorize it.
#
# Give every deployment its OWN X app (a developer account allows 3). X keeps
# at most one live grant per user per client id, so authorizing a second
# deployment through the same app SILENTLY revokes the first one's tokens —
# production quietly logs itself out the next time you run local dev. Never
# copy tokens between deployments either: refresh tokens are single-use and
# rotate, so two holders of one chain invalidate each other. Both rules and
# the evidence behind them: docs/x-api-notes.md N14, N15.
X_OAUTH_CLIENT_ID=
X_OAUTH_CLIENT_SECRET=

# ---- Gating a deployment (deployed only) -------------------------------

# A deployed Worker holding a working X token is a way for anyone with the URL
# to spend your money, so one of the two blocks below has to be set before it
# will serve /api/*. Neither applies to localhost, which is never gated; the
# Bun server binds only to 127.0.0.1.
#
# Set these once the Worker sits behind Cloudflare Access. Both values appear
# in the modal shown when you enable it — which is why they come after the
# first deploy rather than during it. The Worker then verifies the Access JWT
# on every request that reaches it, so the API fails closed if Access is ever
# turned off. See DEPLOYING.md.
# POLICY_AUD=
# TEAM_DOMAIN=https://<your-team>.cloudflareaccess.com

# Or say out loud that you want no gate at all — a Worker anyone can reach and
# spend through. Only the exact string "true" counts, so a typo leaves the
# gate up.
# ALLOW_UNGATED=true

# ---- Optional: tuning (defaults shown; uncomment to change) ------------

# Cap on main conversation-search results per run, 10–5000. The default 500 is
# at most $2.50 gross for those main results, not the whole action: referenced
# posts and follow-up media/root/quote lookups can add billed reads. A malformed
# value refuses to boot rather than removing the bound. The default five-page,
# no-ancillary fixture fits D1 Free's query budget; a full 5000-result run does
# not. On Workers, update it with
# `bunx wrangler secret put MAX_POSTS_PER_FETCH`; the new value applies at once.
# MAX_POSTS_PER_FETCH=500

# Port for the loopback-only Bun dev server; must match the registered OAuth
# callback.
# PORT=8788

# Same, for scripts/dev-worker.sh (`wrangler dev`). Separate from PORT because
# the two runtimes are started by different things; they default to the same
# port, so only one may run at a time unless you change this.
# WORKER_PORT=8788

# SQLite file for local dev (the Worker uses D1 instead).
# DB_PATH=data/x-threaded.sqlite
